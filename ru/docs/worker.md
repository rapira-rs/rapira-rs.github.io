---
title: Режим Worker
description: "Как написать скрипт воркера Rapira: резидентный цикл, контракт handle_request(), что живёт между запросами и типичные подводные камни."
faqLevel: 2
---

# Режим Worker

Режим Worker оставляет процесс PHP живым между запросами: скрипт один раз поднимает приложение, а затем уходит в цикл, где на каждой итерации просит у Rapira очередной запрос. Старт выполняется один раз, при запуске, и каждый следующий запрос попадает в уже прогретое приложение. Состояние тоже живёт дольше запроса, поэтому скрипт воркера должен им управлять.

В [режиме Classic](/ru/docs/classic) входной скрипт, наоборот, выполняется с нуля на каждом запросе, и всё, что он успел построить, отбрасывается, как только на запрос дан ответ, поэтому поднять современный фреймворк - автозагрузчик, контейнер, конфигурацию, маршруты, соединения с базой - стоит одинаково на каждом запросе.

Эта страница - руководство по программированию для режима Worker. Особый фреймворк ему не нужен: достаточно приложения, которое переживёт единственный старт и множество запросов после него, а на это способно большинство современных фреймворков. Все три режима и то, от чего зависит доступный приложению режим, описывают [Режимы выполнения](/ru/docs/execution-modes), а руководства по конкретным фреймворкам собраны в разделе [Фреймворки](/ru/docs/frameworks/).

## Резидентный цикл

Скрипт воркера состоит из трёх частей: всё, что вы поднимаете в начале файла, обработчик, отвечающий на один запрос, и цикл, который вызывает обработчик, пока воркер не пойдёт на завершение. Цикл пишется на PHP вокруг свободной функции `\Rapira\handle_request()`.

```php
<?php
// worker.php
require __DIR__ . '/vendor/autoload.php';

$app = new App(); // The worker creates this object once and reuses it.

$handler = static function () use ($app): void {
    header('Content-Type: text/plain');
    http_response_code(200);
    echo $app->handle($_SERVER['REQUEST_URI']);
};

while (\Rapira\handle_request($handler)) {
    gc_collect_cycles();
}
```

По умолчанию Rapira работает в режиме Dispatcher. Режим Worker включается двумя способами, и оба делают одно и то же:

- `--mode worker` в командной строке, рядом с входным скриптом.
- `mode = "worker"` в секции `[pool]` файла `rapira.toml`.

```bash
rapira serve --mode worker app/worker.php
```

Остальные флаги собраны в разделе [Командная строка](/ru/docs/cli), а их аналоги для `rapira.toml` - в [Конфигурации](/ru/docs/configuration).

## Контракт `handle_request()`

`\Rapira\handle_request(callable $handler): bool` - это весь контракт целиком:

- **Блокируется** до тех пор, пока воркеру не достанется запрос. Пока воркер ждёт в `handle_request()`, процессор он не расходует и при этом продолжает держать в памяти свой интерпретатор и поднятое приложение.
- **Заполняет суперглобальные переменные** - `$_GET`, `$_POST`, `$_SERVER`, `$_COOKIE` и всё остальное семейство - данными пришедшего запроса, заново и до того, как выполнится ваш обработчик. Обычный PHP-код, который их читает, работает точно так же, как под php-fpm.
- **Вызывает ваш обработчик без единого аргумента.** Всё о запросе уже лежит в суперглобальных переменных, поэтому сигнатура колбэка - `function (): void`. Всё остальное, что ему нужно - контейнер, приложение, логгер, - захватывайте через `use`.
- **Ответом становится ваш вывод.** `echo`, `print`, `header()`, `http_response_code()`, `setcookie()` - обработчик формирует ответ ровно так же, как классический скрипт. Как связаны данные запроса и вывод ответа, разбирает раздел [HTTP](/ru/docs/http).
- **Возвращает `true`**, когда запрос закончен, то есть цикл продолжается. **`false`** приходит тогда, когда воркер пошёл на завершение. На этом и держится цикл: как только вернулось `false`, выходите из него и дайте скрипту закончиться.
- **Живёт на верхнем уровне входного скрипта.** Вызывайте её из собственного цикла скрипта и больше ниоткуда: поведение вызова из shutdown-функции или из деструктора не определено.

Получается, что запрос в режиме Worker - это одна итерация вашего цикла `while`. Rapira закрывает запрос вокруг обработчика: отрабатывают shutdown-функции, которые зарегистрировал этот запрос, буферы вывода сбрасываются и обнуляются, сессия записывается и закрывается, а суперглобальные переменные наполняются заново к следующей итерации. Всё, что скрипт держит за пределами обработчика, остаётся ровно там же, где было. Прохода по деструкторам в конце запроса Rapira не делает: объект уничтожается тогда, когда исчезает последняя ссылка на него.

## Один обработчик на воркер

`handle_request()` возвращает управление после каждого запроса, а не обслуживает их бесконечно, поэтому воркер живёт благодаря циклу вокруг вызова, и предоставить этот цикл должен сам скрипт воркера.

Значит, скрипт воркера обслуживает ровно один обработчик за раз. Если написать два цикла подряд, до второго дело не дойдёт, пока не завершится первый, а первый завершится только тогда, когда `handle_request()` вернёт `false`, то есть когда воркер уже уходит. Разводить запросы по разным веткам кода должен сам обработчик, внутри себя, а не несколько циклов.

```php
while (\Rapira\handle_request($api)) {
}

// Code reaches this loop only during shutdown.
while (\Rapira\handle_request($web)) {
}
```

## Что живёт между запросами

Всё, что вы создаёте **вне** обработчика, живёт столько же, сколько процесс воркера: автозагрузчик, DI-контейнер, скомпилированные маршруты, конфигурация, открытые соединения с базой и кешем, прогретые кеши. Ничто из этого не пересоздаётся на каждом запросе.

Всё, что вы создаёте **внутри** обработчика, - обычная работа на один запрос: она освобождается, когда обработчик возвращает управление и исчезает последняя ссылка на неё.

Где проходит граница между этими двумя частями - проектное решение скрипта воркера: состояние, задуманное общим, размещайте выше цикла, а состояние одного запроса оставляйте в обработчике или сбрасывайте до прихода следующего.

::: warning
Общим оказывается и глобальное состояние, планировали вы это или нет: статические свойства, синглтоны, реестры, которые библиотека лениво наполняет по ходу дела, `ini_set()`, который так и не отменили. Под php-fpm всё это жило ровно один запрос, потому что завершение запроса в PHP сбрасывало и статику, и глобальные переменные, и `ini_set()`. Воркер Rapira этот сброс между запросами намеренно пропускает, так что они сохраняются. Приложение, которое не может отказаться от глобального состояния, запускается в [режиме Classic](/ru/docs/classic): режим Classic лишает вас прогретого приложения, которое воркер держит в памяти, но остаётся заменой php-fpm без единой правки, а перейти на воркер приложение сможет позже, когда состояние будет распутано.
:::

## Shutdown-функции

Shutdown-функция, зарегистрированная при старте, вне цикла, выполняется один раз - в конце жизненного цикла воркера, то есть обычно при его выходе. В конце каждого запроса она не выполняется. Shutdown-функция, которую ваш обработчик зарегистрировал по ходу запроса, выполняется в конце этого запроса, один раз, и больше не повторяется.

Уборку ресурсов, общих для всего процесса, регистрируйте при старте, а уборку того, что принадлежит одному запросу, - внутри обработчика.

```php
register_shutdown_function(static function (): void {
    // Runs once when the worker cycle ends.
});

$handler = static function (): void {
    register_shutdown_function(static function (): void {
        // Runs at the end of this request.
    });
};

while (\Rapira\handle_request($handler)) {
}
```

В конце цикла первыми отрабатывают стартовые регистрации, в порядке регистрации. Функция, зарегистрированная скриптом уже после цикла, выполняется после них.

С объектами правило другое. Прохода по деструкторам в конце запроса Rapira не делает. Объект уничтожается, когда исчезает последняя ссылка на него, поэтому объект, за который держится только локальная переменная обработчика, уничтожается на выходе из обработчика. Объект, за который держится переменная уровня старта, остаётся в памяти между запросами, и его `__destruct()` выполнится один раз - в конце жизненного цикла воркера.

::: question Почему shutdown-функция, зарегистрированная при старте, не выполняется в конце первого запроса?
Список shutdown-функций в PHP относится к запросу: завершая запрос, PHP вызывает функции из списка, а затем освобождает сам список. При первом вызове `handle_request()` Rapira забирает стартовые регистрации из этого списка и держит их у себя, поэтому каждый запрос завершается со списком, в котором лежат только его собственные регистрации. В конце цикла Rapira возвращает стартовый список на место и дописывает к нему то, что скрипт зарегистрировал после цикла: итоговое завершение выполняет стартовые записи в порядке регистрации, а более поздние - после них.
:::

## Только в режиме Worker

Функции `handle_request()` нужен резидентный цикл, а он есть только в режиме Worker. В режимах Classic и Dispatcher функция бросает `Rapira\Exception\NotInWorkerModeError`. Каждый класс, который бросает Rapira, реализует маркерный интерфейс `Rapira\Exception\RapiraThrowable`, поэтому одного `catch` хватает на все из них.

`Rapira\get_mode()` возвращает [режим](/ru/docs/execution-modes) текущего процесса как вариант перечисления `Rapira\Mode`. Скрипт, который работает больше чем в одном режиме, читает его до входа в цикл:

```php
if (\Rapira\get_mode() === \Rapira\Mode::Worker) {
    while (\Rapira\handle_request($handler)) {
    }
}
```

## Подводные камни

**Состояние протекает между запросами.** Приложение, которое ведёт себя странно в воркере, но не под php-fpm, обычно протекает состоянием между запросами. Растущий статический массив, объект запроса, осевший в синглтоне, логгер, который держит контекст прошлого пользователя, - каждая такая мелочь проявляется только на втором запросе. Прибирайтесь явно, в начале или в конце обработчика, и сбрасывайте всё, что оставила после себя библиотека. `pool.max_requests` заставляет воркер завершиться после N запросов, чтобы мастер-процесс заменил его свежим: это ограничивает ущерб от медленной утечки, но не устраняет её.

**Несобранные циклические ссылки.** Подсчёт ссылок в PHP освобождает почти всё сразу, но циклические ссылки достаются сборщику циклов и ждут его запуска. Вызов `gc_collect_cycles()` на каждой итерации цикла - как в скрипте выше - не обязателен, но собирает их в предсказуемый момент: между запросами, а не посреди одного из них.

**Запросы, которые никогда не заканчиваются.** Воркер, застрявший в зависшем запросе, остаётся в нём сколь угодно долго и всё это время больше ничего не обрабатывает. `pool.request_terminate_timeout_secs` задаёт предел по реальному времени на один запрос и убивает воркер, который в него не уложился. Этот ключ и `pool.max_requests` описаны в [Конфигурации](/ru/docs/configuration), а что делает мастер-процесс, когда воркер умирает, рассказывает [Модель процессов](/ru/docs/process-model).

**Непойманное исключение затрагивает запрос, а не воркер.** Непойманное исключение в обработчике превращается в ответ `500` - если, конечно, обработчик не успел отправить заголовки ответа до того, как бросил. В любом случае цикл продолжается, так что исключение не уносит воркер с собой. С фатальной ошибкой иначе: она сворачивает резидентный скрипт, поэтому воркер выполняет его заново с самого начала и снова поднимает приложение.

**Работа после ответа.** Если нужно отдать ответ и продолжить работу - разобрать очередь, записать событие в аудит, - ровно это и делает `rapira_finish_request()`. Она описана на странице [HTTP](/ru/docs/http).

## Стаб-файлы для IDE

Функции и классы, которые Rapira отдаёт в PHP, объявлены в стаб-файлах в каталоге `crates/php_sys`. Всё, что относится к воркеру, лежит в [`rapira.stub.php`](https://github.com/rapira-rs/rapira/blob/main/crates/php_sys/rapira.stub.php), а классы исключений - в [`rapira_exception.stub.php`](https://github.com/rapira-rs/rapira/blob/main/crates/php_sys/rapira_exception.stub.php). Это авторитетное описание API: сигнатуры, типы свойств и назначение каждого класса. Они же служат стабами для IDE: положите их в проект, и редактор начнёт подсказывать `\Rapira\handle_request()`, `\Rapira\get_mode()` и всё остальное вместо того, чтобы подчёркивать их как неизвестные.
